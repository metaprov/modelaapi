---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: llmservers.genai.modela.ai
spec:
  group: genai.modela.ai
  names:
    categories:
    - llm
    - modela
    kind: LLMServer
    listKind: LLMServerList
    plural: llmservers
    shortNames:
    - llms
    singular: llmserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.owner
      name: Owner
      priority: 1
      type: string
    - jsonPath: .status.lastSyncAt
      name: Last Sync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LLMServer represents a deployment of a proxy server to one or
          more LLMs
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LLMServerSpec specifies the configuration of the LLM server
            properties:
              authorization:
                description: The security configuration for the LLMServer, which determines
                  how incoming requests are authorized
                properties:
                  allowedKeyGroups:
                    description: The collection of API key groups whose API keys may
                      be used to authorize requests to models served by the LLMServer.
                      AllowedKeyGroups acts as a whitelist, only allowing API keys
                      belonging to a group within the collection to be used. If empty,
                      all API key groups will be allowed to authorize requests to
                      the LLMServer
                    items:
                      type: string
                    type: array
                  allowedKeyNames:
                    description: The collection of API key names that may be used
                      to authenticate requests to models served by the LLMServer.
                      AllowedKeyNames acts as a whitelist, only allowing API keys
                      with names within the collection to be used. If empty, API keys
                      will not be restricted by their names
                    items:
                      type: string
                    type: array
                  insecure:
                    description: Indicates if models served by the LLMServer can be
                      accessed without authorization (not recommended)
                    type: boolean
                type: object
              description:
                default: ""
                description: The user-provided description of the LLM Server
                maxLength: 512
                type: string
              endpoints:
                description: The model endpoints served by the proxy
                items:
                  description: EndpointSpec specifies the serving configuration for
                    a single large language model endpoint
                  properties:
                    format:
                      description: The API format standard that will be served for
                        the model. If unspecified, default to the OpenAI API format
                      type: string
                    model:
                      description: The default LLMServer to use for all internal and
                        consumer-facing completion requests
                      properties:
                        connectionName:
                          description: ConnectionName specifies the name of a connection
                            to a foundational model provider
                          type: string
                        model:
                          description: Model specifies the vendor-specific model type.
                            If unspecified, use the model provided by the `model`
                            option of the connection, or choose the default model
                            for the vendor
                          type: string
                      type: object
                    name:
                      description: The unique name of the model
                      type: string
                    path:
                      description: The path suffix where the model will be served.
                        If unspecified, the default suffix for the LLMServer provider
                        will be used
                      type: string
                    paths:
                      description: The collection of path suffixes where the model
                        will be served
                      items:
                        type: string
                      type: array
                    queryEngine:
                      description: The name of the query engine used to augment requests
                        to the LLMServer. If unspecified, no augmentation will be
                        performed
                      type: string
                  type: object
                type: array
              notification:
                description: The configuration for Alerts generated by the proxy
                properties:
                  notifierName:
                    description: NotifierName specifies the name of a single Notifier
                      resource which the Alert will be forwarded to
                    type: string
                  selector:
                    additionalProperties:
                      type: string
                    description: Selector defines a map of labels that will be matched
                      to find multiple Notifier resource to forward alerts to
                    type: object
                  ttl:
                    default: 3600
                    description: TTL defines the time-to-live for Alerts, in seconds,
                      after which they will be deleted
                    format: int32
                    type: integer
                type: object
              owner:
                default: no-one
                description: Owner specifies the name of the Account which the object
                  belongs to
                type: string
              queryEngines:
                description: The query engines available to the models
                items:
                  description: QueryEngineSpec defines a query engine, which is used
                    to retrieve context for a query and synthesize a response. A query
                    engine may also route queries to other query engines
                  properties:
                    name:
                      description: The unique name of the query engine
                      type: string
                    retriever:
                      description: Retriever augments a query with context from a
                        retriever
                      properties:
                        nodePostProcessors:
                          description: The collection of node postprocessors
                          items:
                            description: NodePostProcessor defines a node post-processor,
                              which mutates the nodes retrieved by a query engine
                              prior to synthesizing a response
                            properties:
                              cohereRerank:
                                description: Rerank the nodes through a specialized
                                  Cohere model
                                properties:
                                  connectionName:
                                    description: The connection to Cohere
                                    type: string
                                  topN:
                                    description: The top number of nodes to return.
                                      If unspecified, default to 2
                                    type: integer
                                type: object
                              embeddingOptimizer:
                                description: Optimize the content of nodes by extracting
                                  the most relevant sentences
                                properties:
                                  embeddingModel:
                                    description: The embedding model to use
                                    properties:
                                      connectionName:
                                        description: ConnectionName specifies the
                                          name of a connection to a foundational model
                                          provider
                                        type: string
                                      model:
                                        description: Model specifies the vendor-specific
                                          model type. If unspecified, use the model
                                          provided by the `model` option of the connection,
                                          or choose the default model for the vendor
                                        type: string
                                    type: object
                                  similarityCutoff:
                                    description: The percentile cutoff, determining
                                      how many top K sentences are selected. If unspecified,
                                      the percentile cutoff will be disabled
                                    type: number
                                  thresholdCutoff:
                                    description: The threshold cutoff for similarity.
                                      If unspecified, the threshold cutoff will be
                                      disabled
                                    type: number
                                type: object
                              embeddingRecency:
                                description: Sort by date and remove similar nodes
                                  that are older than newer ones
                                properties:
                                  embeddingModel:
                                    description: The embedding model to use
                                    properties:
                                      connectionName:
                                        description: ConnectionName specifies the
                                          name of a connection to a foundational model
                                          provider
                                        type: string
                                      model:
                                        description: Model specifies the vendor-specific
                                          model type. If unspecified, use the model
                                          provided by the `model` option of the connection,
                                          or choose the default model for the vendor
                                        type: string
                                    type: object
                                  similarityCutoff:
                                    description: The similarity cutoff. If unspecified,
                                      default to 0.7
                                    type: number
                                type: object
                              fixedRecency:
                                description: Choose the top K nodes sorted by date
                                properties:
                                  topK:
                                    description: The top most recent nodes to return.
                                      If unspecified, default to 1
                                    type: integer
                                type: object
                              keyword:
                                description: Exclude nodes based on keywords present
                                  in their content
                                properties:
                                  exclude:
                                    description: The collection of keywords which
                                      must not be present in the node content. Matching
                                      nodes will be excluded
                                    items:
                                      type: string
                                    type: array
                                  include:
                                    description: The collection of keywords that must
                                      be present in the node content
                                    items:
                                      type: string
                                    type: array
                                type: object
                              llmRerank:
                                description: Rerank the nodes through an LLM
                                properties:
                                  model:
                                    description: The LLM to use for re-ranking. If
                                      unspecified, use the default provided by the
                                      parent specification
                                    properties:
                                      connectionName:
                                        description: ConnectionName specifies the
                                          name of a connection to a foundational model
                                          provider
                                        type: string
                                      model:
                                        description: Model specifies the vendor-specific
                                          model type. If unspecified, use the model
                                          provided by the `model` option of the connection,
                                          or choose the default model for the vendor
                                        type: string
                                    type: object
                                  rankGPT:
                                    description: Indicates if the RankGPT technique
                                      for re-ranking will be used
                                    type: boolean
                                  topN:
                                    description: The top number of nodes to return.
                                      If unspecified, default to 10
                                    type: integer
                                type: object
                              reorder:
                                description: Re-order nodes by their score, placing
                                  the most relevant nodes at the beginning of the
                                  context
                                type: object
                              similarity:
                                description: Exclude nodes below a similarity threshold
                                properties:
                                  cutoff:
                                    description: The threshold cutoff for similarity
                                    type: number
                                type: object
                              timeWeightedRecency:
                                description: Choose the top K nodes after applying
                                  a time-weighted rerank (by creation date or last
                                  access)
                                properties:
                                  lastAccess:
                                    description: Indicates if the time of last access
                                      will be weighted instead of the time of creation.
                                      Documents that have not been queried will be
                                      favored when enabled
                                    type: boolean
                                  timeDecay:
                                    description: The rate at which the time similarity
                                      of a node decays. If unspecified, default 0.1
                                    type: number
                                  topN:
                                    description: The top most recent nodes to return.
                                      If unspecified, default to 1
                                    type: integer
                                type: object
                            type: object
                          type: array
                        responseSynthesizer:
                          description: The response synthesizer to use when generating
                            responses
                          properties:
                            answerFiltering:
                              description: Indicates if answer filtering is enabled.
                                Compatible only with refine and compact response modes
                              type: boolean
                            mode:
                              description: The method to use when synthesizing responses.
                                If unspecified, default to `refine`
                              enum:
                              - refine
                              - compact
                              - simple-summarize
                              - tree-summarize
                              - accumulate
                              - compact-accumulate
                              type: string
                            model:
                              description: The LLM to use when synthesizing responses.
                                If unspecified, use the default provided by the parent
                                specification
                              properties:
                                connectionName:
                                  description: ConnectionName specifies the name of
                                    a connection to a foundational model provider
                                  type: string
                                model:
                                  description: Model specifies the vendor-specific
                                    model type. If unspecified, use the model provided
                                    by the `model` option of the connection, or choose
                                    the default model for the vendor
                                  type: string
                              type: object
                          type: object
                        retriever:
                          description: The name of the retriever to use when retrieving
                            context
                          type: string
                      type: object
                    router:
                      description: Router chooses one or more query engine(s) to route
                        requests to
                      properties:
                        selector:
                          description: The selector to use when choosing query engine(s)
                          properties:
                            mode:
                              description: The selection mode. If unspecified, default
                                to `genai`
                              enum:
                              - embedding
                              - llm
                              type: string
                            model:
                              description: The LLM or embedding model that will be
                                used to determine the selections. If unspecified,
                                use the default LLM provided by the model server
                              properties:
                                connectionName:
                                  description: ConnectionName specifies the name of
                                    a connection to a foundational model provider
                                  type: string
                                model:
                                  description: Model specifies the vendor-specific
                                    model type. If unspecified, use the model provided
                                    by the `model` option of the connection, or choose
                                    the default model for the vendor
                                  type: string
                              type: object
                            multi:
                              description: Indicates if multiple options may be selected
                              type: boolean
                          type: object
                        tools:
                          description: The query engines to route requests to
                          items:
                            properties:
                              description:
                                description: The description of the query engine
                                type: string
                              name:
                                description: The name of the query engine
                                type: string
                              queryEngine:
                                description: The name of the query engine
                                type: string
                            type: object
                          type: array
                      type: object
                    subQuestion:
                      description: SubQuestion routes sub-questions generated for
                        a query to one or more query engine(s)
                      properties:
                        model:
                          description: The model to use when generating questions
                          properties:
                            connectionName:
                              description: ConnectionName specifies the name of a
                                connection to a foundational model provider
                              type: string
                            model:
                              description: Model specifies the vendor-specific model
                                type. If unspecified, use the model provided by the
                                `model` option of the connection, or choose the default
                                model for the vendor
                              type: string
                          type: object
                        responseSynthesizer:
                          description: The response synthesizer to use when generating
                            responses
                          properties:
                            answerFiltering:
                              description: Indicates if answer filtering is enabled.
                                Compatible only with refine and compact response modes
                              type: boolean
                            mode:
                              description: The method to use when synthesizing responses.
                                If unspecified, default to `refine`
                              enum:
                              - refine
                              - compact
                              - simple-summarize
                              - tree-summarize
                              - accumulate
                              - compact-accumulate
                              type: string
                            model:
                              description: The LLM to use when synthesizing responses.
                                If unspecified, use the default provided by the parent
                                specification
                              properties:
                                connectionName:
                                  description: ConnectionName specifies the name of
                                    a connection to a foundational model provider
                                  type: string
                                model:
                                  description: Model specifies the vendor-specific
                                    model type. If unspecified, use the model provided
                                    by the `model` option of the connection, or choose
                                    the default model for the vendor
                                  type: string
                              type: object
                          type: object
                        tools:
                          description: The query engines to route requests to
                          items:
                            properties:
                              description:
                                description: The description of the query engine
                                type: string
                              name:
                                description: The name of the query engine
                                type: string
                              queryEngine:
                                description: The name of the query engine
                                type: string
                            type: object
                          type: array
                      type: object
                  type: object
                type: array
              resources:
                description: The resource requirements that will be allocated to the
                  proxy deployment
                properties:
                  custom:
                    default: false
                    description: Custom indicates if the custom requirements specified
                      by Requirements will be used
                    type: boolean
                  requirements:
                    description: Requirements specifies the custom resource requirements
                      for the workload
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: 'Limits describes the maximum amount of compute
                          resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: 'Requests describes the minimum amount of compute
                          resources required. If Requests is omitted for a container,
                          it defaults to Limits if that is explicitly specified, otherwise
                          to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                        type: object
                    type: object
                  workloadName:
                    description: WorkloadName references the name of a Workload Class
                      resource that will be used to determine the resource requirements
                      of the workload. If specified, WorkloadName will take precedence
                      over custom requirements
                    type: string
                type: object
              retrievers:
                description: The collection of retrievers available to the query engines
                items:
                  description: RetrieverSpec defines a retriever, which collects text
                    chunks relevant to a query. A retriever may also route queries
                    to other retrievers
                  properties:
                    documentSummary:
                      description: DocumentSummary retrieves nodes from a document
                        summary index
                      properties:
                        index:
                          description: The reference to the vector store index
                          properties:
                            index:
                              description: The name of the index. If unspecified,
                                default to the first index defined by the Knowledge
                                Base
                              type: string
                            knowledgeBaseName:
                              description: The name of the Knowledge Base
                              type: string
                          type: object
                        mode:
                          description: The retriever mode. If unspecified, default
                            to embedding
                          enum:
                          - embedding
                          - llm
                          type: string
                        model:
                          description: The embedding or large language model to use.
                            If unspecified, use the default provided by model server
                          properties:
                            connectionName:
                              description: ConnectionName specifies the name of a
                                connection to a foundational model provider
                              type: string
                            model:
                              description: Model specifies the vendor-specific model
                                type. If unspecified, use the model provided by the
                                `model` option of the connection, or choose the default
                                model for the vendor
                              type: string
                          type: object
                        topK:
                          description: The top K nodes to retrieve. If unspecified,
                            default to 1
                          type: integer
                      required:
                      - index
                      type: object
                    fusion:
                      description: Fusion combines the results of one or more retriever(s)
                      properties:
                        defaultScore:
                          description: The default score for nodes which were retrieved
                            without a score. If unspecified, default to 1
                          type: number
                        mode:
                          description: The fusion retriever mode. If unspecified,
                            default to simple
                          enum:
                          - reciprocal-rerank
                          - relative-score
                          - distance-based-score
                          - simple
                          type: string
                        model:
                          description: The embedding or large language model to use
                            when generating questions. If unspecified, use the default
                            provided by model server
                          properties:
                            connectionName:
                              description: ConnectionName specifies the name of a
                                connection to a foundational model provider
                              type: string
                            model:
                              description: Model specifies the vendor-specific model
                                type. If unspecified, use the model provided by the
                                `model` option of the connection, or choose the default
                                model for the vendor
                              type: string
                          type: object
                        queries:
                          description: The number of queries to generate for the input
                            query. If unspecified, default to 4
                          type: integer
                        retrievers:
                          description: The collection of retriever names
                          items:
                            type: string
                          type: array
                        topK:
                          description: The top K nodes to retrieve. If unspecified,
                            default to 1
                          type: integer
                      type: object
                    keywordTable:
                      description: KeywordTable retrieves nodes from a keyword table
                        index
                      properties:
                        chunksPerQuery:
                          description: The maximum number of chunks to retrieve per
                            query. If unspecified, default to 10
                          type: integer
                        index:
                          description: The reference to the tree index
                          properties:
                            index:
                              description: The name of the index. If unspecified,
                                default to the first index defined by the Knowledge
                                Base
                              type: string
                            knowledgeBaseName:
                              description: The name of the Knowledge Base
                              type: string
                          type: object
                        keywordsPerQuery:
                          description: The maximum number of keywords to consider
                            per query. If unspecified, default to 10
                          type: integer
                        mode:
                          description: The retriever mode. If unspecified, default
                            to RAKE
                          enum:
                          - simple
                          - llm
                          - rake
                          type: string
                        model:
                          description: The large language model to use, if using the
                            LLM mode. If unspecified, use the default provided by
                            model server
                          properties:
                            connectionName:
                              description: ConnectionName specifies the name of a
                                connection to a foundational model provider
                              type: string
                            model:
                              description: Model specifies the vendor-specific model
                                type. If unspecified, use the model provided by the
                                `model` option of the connection, or choose the default
                                model for the vendor
                              type: string
                          type: object
                      required:
                      - index
                      type: object
                    list:
                      description: List retrievers nodes from a list index
                      properties:
                        index:
                          description: The reference to the tree index
                          properties:
                            index:
                              description: The name of the index. If unspecified,
                                default to the first index defined by the Knowledge
                                Base
                              type: string
                            knowledgeBaseName:
                              description: The name of the Knowledge Base
                              type: string
                          type: object
                        mode:
                          description: The retriever mode. If unspecified, default
                            to simple
                          enum:
                          - simple
                          - embedding
                          - llm
                          type: string
                        model:
                          description: The large language or embedding model to use,
                            if applicable. If unspecified, use the default provided
                            by model server
                          properties:
                            connectionName:
                              description: ConnectionName specifies the name of a
                                connection to a foundational model provider
                              type: string
                            model:
                              description: Model specifies the vendor-specific model
                                type. If unspecified, use the model provided by the
                                `model` option of the connection, or choose the default
                                model for the vendor
                              type: string
                          type: object
                        topK:
                          description: The top K nodes to retrieve when using the
                            embedding or LLM mode. If unspecified, default to 1
                          type: integer
                      required:
                      - index
                      type: object
                    name:
                      description: The unique name of the retriever
                      type: string
                    router:
                      description: Router chooses one or more retriever(s) to route
                        requests to
                      properties:
                        selector:
                          description: The selector to use when choosing retriever(s)
                          properties:
                            mode:
                              description: The selection mode. If unspecified, default
                                to `genai`
                              enum:
                              - embedding
                              - llm
                              type: string
                            model:
                              description: The LLM or embedding model that will be
                                used to determine the selections. If unspecified,
                                use the default LLM provided by the model server
                              properties:
                                connectionName:
                                  description: ConnectionName specifies the name of
                                    a connection to a foundational model provider
                                  type: string
                                model:
                                  description: Model specifies the vendor-specific
                                    model type. If unspecified, use the model provided
                                    by the `model` option of the connection, or choose
                                    the default model for the vendor
                                  type: string
                              type: object
                            multi:
                              description: Indicates if multiple options may be selected
                              type: boolean
                          type: object
                        tools:
                          description: The query engines to route requests to
                          items:
                            properties:
                              description:
                                description: The description of the retriever
                                type: string
                              name:
                                description: The name of the retriever
                                type: string
                              retriever:
                                description: The name of the retriever
                                type: string
                            type: object
                          type: array
                      type: object
                    tree:
                      description: Tree retrieves nodes from a tree index
                      properties:
                        childBranchFactor:
                          description: The number of child nodes to consider traversing
                            at each level of the tree, if applicable
                          type: integer
                        index:
                          description: The reference to the tree index
                          properties:
                            index:
                              description: The name of the index. If unspecified,
                                default to the first index defined by the Knowledge
                                Base
                              type: string
                            knowledgeBaseName:
                              description: The name of the Knowledge Base
                              type: string
                          type: object
                        mode:
                          description: The retriever mode. If unspecified, default
                            to select leaf
                          enum:
                          - select-leaf
                          - select-leaf-embedding
                          - root
                          - all-leaf
                          type: string
                        model:
                          description: The large language model to use, if applicable.
                            If unspecified, use the default provided by model server
                          properties:
                            connectionName:
                              description: ConnectionName specifies the name of a
                                connection to a foundational model provider
                              type: string
                            model:
                              description: Model specifies the vendor-specific model
                                type. If unspecified, use the model provided by the
                                `model` option of the connection, or choose the default
                                model for the vendor
                              type: string
                          type: object
                      required:
                      - index
                      type: object
                    vector:
                      description: Vector retrieves nodes from a vector index
                      properties:
                        auto:
                          description: The specification to automatically configure
                            queries to the vector index
                          properties:
                            enabled:
                              description: Indicates if automatic query generation
                                is enabled
                              type: boolean
                            maxTopK:
                              description: The maximum top K allowed. The top K set
                                by the LLM will be clamped to this value
                              type: integer
                            metadataInfo:
                              description: The collection of metadata key info, which
                                the LLM will use to infer metadata filters
                              items:
                                properties:
                                  description:
                                    description: The key description
                                    type: string
                                  name:
                                    description: The name of the key
                                    type: string
                                  type:
                                    description: The key type (integer, string, float,
                                      etc.)
                                    type: string
                                type: object
                              type: array
                            model:
                              description: The LLM to use when generating queries.
                                If unspecified, use the default provided by model
                                server
                              properties:
                                connectionName:
                                  description: ConnectionName specifies the name of
                                    a connection to a foundational model provider
                                  type: string
                                model:
                                  description: Model specifies the vendor-specific
                                    model type. If unspecified, use the model provided
                                    by the `model` option of the connection, or choose
                                    the default model for the vendor
                                  type: string
                              type: object
                          required:
                          - enabled
                          type: object
                        autoMerging:
                          description: Indicates if auto-merging of parent and child
                            nodes will be enabled. Auto-merging should only be enabled
                            on indexes that were constructed using a hierarchical
                            node parser. If unspecified, default to false
                          type: boolean
                        embeddingModel:
                          description: The embedding model to use
                          properties:
                            connectionName:
                              description: ConnectionName specifies the name of a
                                connection to a foundational model provider
                              type: string
                            model:
                              description: Model specifies the vendor-specific model
                                type. If unspecified, use the model provided by the
                                `model` option of the connection, or choose the default
                                model for the vendor
                              type: string
                          type: object
                        index:
                          description: The reference to the vector store index
                          properties:
                            index:
                              description: The name of the index. If unspecified,
                                default to the first index defined by the Knowledge
                                Base
                              type: string
                            knowledgeBaseName:
                              description: The name of the Knowledge Base
                              type: string
                          type: object
                        matchMetadata:
                          additionalProperties:
                            type: string
                          description: Metadata key-value pairs that must be matched
                            on retrieved nodes
                          type: object
                        topK:
                          description: The top K nodes to retrieve. If unspecified,
                            default to 10
                          type: integer
                      required:
                      - index
                      type: object
                  type: object
                type: array
              servingSiteName:
                description: The name of the Serving Site under which the LLM Server
                  proxy deployment will be created. If empty, default to the Data
                  Product's default Serving Site
                type: string
            type: object
          status:
            properties:
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    type FooStatus struct{ // Represents the observations of a foo's
                    current state. // Known .status.conditions.type are: \"Available\",
                    \"Progressing\", and \"Degraded\" // +patchMergeKey=type // +patchStrategy=merge
                    // +listType=map // +listMapKey=type Conditions []metav1.Condition
                    `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\"
                    protobuf:\"bytes,1,rep,name=conditions\"` \n // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deployedAt:
                description: DeployedAt specifies the time that the LLMServer proxy
                  deployment was created
                format: date-time
                type: string
              deploymentRef:
                description: The reference to the Kubernetes Deployment that manages
                  the pods for the prediction proxy
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: 'If referring to a piece of an object instead of
                      an entire object, this string should contain a valid JSON/Go
                      field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within
                      a pod, this would take on a value like: "spec.containers{name}"
                      (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]"
                      (container with index 2 in this pod). This syntax is chosen
                      only to have some well-defined way of referencing a part of
                      an object. TODO: this design is not final and this field is
                      subject to change in the future.'
                    type: string
                  kind:
                    description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                  namespace:
                    description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                    type: string
                  resourceVersion:
                    description: 'Specific resourceVersion to which this reference
                      is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency'
                    type: string
                  uid:
                    description: 'UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              endpoint:
                description: Endpoint contains the URL where the proxy server is exposed
                type: string
              endpoints:
                description: Endpoints contains the collection of statuses for each
                  model served by the proxy server
                items:
                  description: EndpointStatus describes the current state of a single
                    model served by the LLMServer proxy
                  properties:
                    failureMessage:
                      description: The details of the last failure that occurred while
                        deploying the model
                      type: string
                    metrics:
                      description: The metrics collected for the model
                      properties:
                        p50:
                          description: 50% latency for requests served by the endpoint
                          type: number
                        p95:
                          description: 95% latency for requests served by the endpoint
                          type: number
                        p99:
                          description: 99% latency for requests served by the endpoint
                          type: number
                        totalCost:
                          description: The total approximate cost (in USD) of tokens
                            served by the endpoint
                          type: number
                        totalRequests:
                          description: The total number of requests served by the
                            endpoint
                          format: int32
                          type: integer
                        totalTokens:
                          description: The total number of tokens served by the endpoint
                          format: int32
                          type: integer
                      type: object
                    name:
                      description: The unique name of the model
                      type: string
                  type: object
                type: array
              failureMessage:
                description: The details of the failure that occurred while deploying
                  the prediction proxy
                type: string
              observedGeneration:
                description: ObservedGeneration specifies the last generation that
                  was reconciled
                format: int64
                type: integer
              serviceRef:
                description: The reference to the Kubernetes Service which exposes
                  the prediction proxy
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: 'If referring to a piece of an object instead of
                      an entire object, this string should contain a valid JSON/Go
                      field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within
                      a pod, this would take on a value like: "spec.containers{name}"
                      (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]"
                      (container with index 2 in this pod). This syntax is chosen
                      only to have some well-defined way of referencing a part of
                      an object. TODO: this design is not final and this field is
                      subject to change in the future.'
                    type: string
                  kind:
                    description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                  namespace:
                    description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                    type: string
                  resourceVersion:
                    description: 'Specific resourceVersion to which this reference
                      is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency'
                    type: string
                  uid:
                    description: 'UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              updatedAt:
                description: The last time the object was updated
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
