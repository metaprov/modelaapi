
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: models.training.modeld.io
spec:
  group: training.modeld.io
  names:
    categories:
    - training
    - modeld
    - all
    kind: Model
    listKind: ModelList
    plural: models
    shortNames:
    - md
    singular: model
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .spec.versionName
      name: Version
      type: string
    - jsonPath: .spec.studyName
      name: Study
      type: string
    - jsonPath: .spec.task
      name: Task
      type: string
    - jsonPath: .spec.estimator.algorithmName
      name: Algorithm
      type: string
    - jsonPath: .spec.objective
      name: Objective
      type: string
    - jsonPath: .status.cvScore
      name: CV Score
      type: number
    - jsonPath: .status.trainScore
      name: Train Score
      type: number
    - jsonPath: .status.testScore
      name: Test Score
      type: number
    - jsonPath: .status.best
      name: Best
      type: boolean
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ==============================================================================
          PublishedModelRef Configuration ==============================================================================
          Model represent a machine learning model. models are automatically generated
          by the planner and trained by the trainer. After training, models are packaged
          into a docker container and served by predictor.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ModelSpec defines the desired state of the Model resource
            properties:
              aborted:
                description: Aborted indicate the desire to abort the model
                type: boolean
              archived:
                description: Set when the model should be archived
                type: boolean
              datasetName:
                description: DatasetName refer to the dataset object for which the
                  study is for. Required.
                type: string
              dnn:
                description: DeepNet estimator specification. Not supported for this
                  release.
                properties:
                  architecture:
                    description: Architecture is the dnn architue
                    items:
                      properties:
                        inputLayers:
                          description: Input layers are the layer input to this layer
                          items:
                            type: string
                          type: array
                        name:
                          description: FileName is the name of the layer
                          type: string
                        parameters:
                          description: Parameters are the hyper parameters of this
                            layer
                          items:
                            properties:
                              name:
                                type: string
                              value:
                                type: string
                            required:
                            - name
                            - value
                            type: object
                          type: array
                        type:
                          description: The type if the layer type
                          type: string
                      required:
                      - inputLayers
                      - name
                      - parameters
                      - type
                      type: object
                    type: array
                  batchSize:
                    description: Batch size specify about
                    format: int32
                    type: integer
                  epochs:
                    description: Budget size specify about
                    format: int32
                    type: integer
                  gpus:
                    description: Gpus denote of gpu for trainer, any number more than
                      1 denote distribute training
                    format: int32
                    type: integer
                  isSeq:
                    description: Is this a seq model
                    type: boolean
                  loss:
                    description: compile
                    type: string
                  type:
                    description: The type of the dnn
                    type: string
                  validationSplit:
                    description: ValidationSplit specify about
                    format: int32
                    type: integer
                type: object
              ensemble:
                description: 'If this is an ensemble model, specify the ensemble Default:
                  None'
                properties:
                  base:
                    description: A list of base model to ensemble. The ensemble method
                      is represented as the algorithm name.
                    items:
                      type: string
                    type: array
                type: object
              estimator:
                description: Specification of the ML algorithm and its hyper parameters.
                properties:
                  algorithmName:
                    description: A reference to the algorithm in the catalog
                    type: string
                  parameters:
                    description: The algorithm hyper parameters
                    items:
                      description: HyperParameterValue represent a specific value
                        of the learning algorithm hyper-parameter. The value is chosen
                        by the planner
                      properties:
                        name:
                          description: Reference to the name of the hyper parameter.
                          type: string
                        value:
                          description: Value hold the value if  the hyper paramer
                            type is string
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              forecasted:
                description: Set the true when the model should perform a forecast
                type: boolean
              forecastingSpec:
                description: The specification for the forecasting algorithm if this
                  is a forecast study.
                properties:
                  backtest:
                    description: The backtest specification, the system supports back
                      testing with expanding windows.
                    properties:
                      initial:
                        description: The initial number of data points, default to
                          80% of rows.
                        format: int32
                        type: integer
                      windows:
                        description: The number of backtesting windows. Default to
                          3. can be from 1 to 5.
                        format: int32
                        type: integer
                    type: object
                  confidenceIntervals:
                    description: The confidence levels for the forecast, each level
                      must be between 1-100.
                    format: int32
                    type: integer
                  countryForHoliday:
                    description: Set an holiday schedule for a country.
                    enum:
                    - US
                    - BR
                    - ID
                    - IN
                    - MY
                    - VN
                    - TH
                    - PH
                    - PK
                    - BD
                    - EG
                    - CN
                    - RU
                    - KR
                    - BY
                    - AE
                    - none
                    type: string
                  datetimeFormat:
                    description: The format of the datetime column. Used default
                    type: string
                  dimensionValues:
                    description: a set of value for each partition key
                    items:
                      description: Partition key values are used for the partition
                      properties:
                        key:
                          description: Key is the partition key
                          type: string
                        value:
                          description: Value if the partition value
                          type: string
                      type: object
                    type: array
                  dimensions:
                    description: The list of the dimension columns. If non, the system
                      will treat the whole time series as one.
                    items:
                      type: string
                    type: array
                  forecast:
                    description: Specify if we should generate a forecast using the
                      model If true, the system will perform a forecast and update
                      the forecast connection. Default it true
                    type: boolean
                  forecastConnectionName:
                    description: The name of the connection for a database the result
                      of the forecast If null, the system will insert the forecast
                      in the database.
                    type: string
                  freqSpec:
                    description: Required, the freq of the time series (daily,weekly)
                    properties:
                      interval:
                        description: Default to 1. optional
                        format: int32
                        type: integer
                      unit:
                        description: required
                        enum:
                        - second
                        - minute
                        - hour
                        - day
                        - week
                        - month
                        - quarter
                        - year
                        type: string
                    type: object
                  horizon:
                    description: number of data points to predict in the future. Required.
                    format: int32
                    type: integer
                  repressors:
                    description: List of other columns to take into consideration
                      Default None
                    items:
                      type: string
                    type: array
                  targetColumn:
                    description: The name of the column holding the value. By default
                      this is the target column from the dataset.
                    type: string
                  timeColumn:
                    description: The name of the time column Required.
                    type: string
                type: object
              location:
                description: The location of the model artifacts (metadata, reports
                  and estimators).
                properties:
                  bucketName:
                    type: string
                  path:
                    description: Path to the full data file (e.g. csv file).
                    type: string
                required:
                - bucketName
                - path
                type: object
              objective:
                description: 'Objective is the metric by which the system compare
                  models Default: based on the task. classification is logloss. Regression
                  is rmse'
                enum:
                - accuracy
                - average-precision
                - balanced-accuracy
                - brier-score-loss
                - f1
                - f1-macro
                - f1-micro
                - f1-weighted
                - log-loss
                - precision
                - precision-micro
                - precision-macro
                - precision-weighted
                - recall
                - recall-macro
                - recall-micro
                - recall-weighted
                - auc
                - fp
                - fn
                - tn
                - tp
                - explained_variance
                - mae
                - mse
                - msle
                - median-absolute-error
                - r2
                - adjusted-mutual-info-score
                - adjusted-rand-score
                - completeness-score
                - fowlkes-mallows-score
                - homogeneity-score
                - mutual-info-score
                - normalized-mutual-info-score
                - v-measure-score
                - rmse
                - unknown
                - mape
                - smape
                - mdape
                type: string
              owner:
                description: The owner account name
                type: string
              ownerName:
                description: The account name of the owner of this model
                type: string
              paused:
                description: Paused is set when we want to pause the training
                type: boolean
              preprocessing:
                description: 'Represent the preprocessing pipeline of the model. Provide
                  a value if you want to customize the model. Default: All preprocessing
                  will be created automatically'
                properties:
                  audio:
                    description: Specify the pipeline for audio. Not supported in
                      this release
                    properties:
                      columns:
                        description: Name of the datetime columns
                        items:
                          type: string
                        type: array
                      featurizer:
                        description: The date time imputer.
                        type: string
                    type: object
                  categorical:
                    description: One or more categorical pipelines.
                    properties:
                      columns:
                        description: The columns for this pipeline. Use * to denotes
                          all the columns. Must have at least on value.
                        items:
                          type: string
                        type: array
                      encoder:
                        description: CatEncoder
                        enum:
                        - one-hot
                        - count-freq
                        - ordinal-encoder
                        - mean
                        - target
                        - woe
                        - rare-label
                        - binary
                        - label
                        - none
                        - auto
                        type: string
                      imputer:
                        description: Categorical varaible imputer
                        enum:
                        - remove-rows-with-missing-values
                        - mean
                        - median
                        - mode
                        - arbitrary-number
                        - end-tail
                        - random-sample
                        - add-missing-indication
                        - iterative
                        - none
                        - auto
                        - constant
                        type: string
                    type: object
                  datatime:
                    description: Specify the column transformation for datetime columns
                    properties:
                      columns:
                        description: Name of the datetime columns
                        items:
                          type: string
                        type: array
                      expand:
                        description: If
                        type: boolean
                      imputer:
                        description: The date time imputer.
                        enum:
                        - remove-rows-with-missing-values
                        - mean
                        - median
                        - mode
                        - arbitrary-number
                        - end-tail
                        - random-sample
                        - add-missing-indication
                        - iterative
                        - none
                        - auto
                        - constant
                        type: string
                    type: object
                  featureSelector:
                    description: FeaturePipeline selector
                    enum:
                    - select-percentile
                    - select-kbest
                    - select-fpr
                    - select-fdr
                    - variance-treshold
                    - none
                    - auto
                    type: string
                  image:
                    description: Specify the pipeline for images. Not supported in
                      this release
                    properties:
                      columns:
                        description: Name of the datetime columns
                        items:
                          type: string
                        type: array
                      featurizer:
                        description: The date time imputer.
                        type: string
                    type: object
                  imbalanced:
                    description: Imbalance flag instruct the designer to create an
                      imbalanced pipeline. Default is false
                    type: boolean
                  numeric:
                    description: Specify the column transformation for numeric columns
                    properties:
                      columns:
                        items:
                          type: string
                        type: array
                      imputer:
                        description: Numerical var imputer
                        enum:
                        - remove-rows-with-missing-values
                        - mean
                        - median
                        - mode
                        - arbitrary-number
                        - end-tail
                        - random-sample
                        - add-missing-indication
                        - iterative
                        - none
                        - auto
                        - constant
                        type: string
                      scaler:
                        description: Numeric var scaler
                        enum:
                        - standard
                        - max-abs
                        - min-max
                        - normalizer
                        - robust
                        - none
                        - auto
                        type: string
                    type: object
                  reducer:
                    description: Dim reducer
                    enum:
                    - pca
                    - kernel-pca
                    - fast-ica
                    - none
                    - auto
                    type: string
                  text:
                    description: Specify the column transformation for text columns
                    properties:
                      columns:
                        description: The list of test columns
                        items:
                          type: string
                        type: array
                      embedding:
                        description: If true, the controller will Add word embedding
                          handling to the text pipeline.
                        type: string
                      encoder:
                        description: The text encoder (e.g. tfidf)
                        type: string
                      lemma:
                        description: If true, the controller will Add lemma handling
                          to the text pipeline.
                        type: boolean
                      pos:
                        description: If true, the controller will Add part of speech
                          handling to the text pipeline.
                        type: boolean
                      stem:
                        description: If true, the controller will Add stemmer handling
                          to the text pipeline.
                        type: boolean
                      stopwords:
                        description: If true, the controller will Add stop word handling
                          to the text pipeline.
                        type: boolean
                      tokenizer:
                        description: Text tokenizer
                        type: string
                    type: object
                  video:
                    description: Specify the pipeline for video. Not supported in
                      this release
                    properties:
                      columns:
                        description: Name of the datetime columns
                        items:
                          type: string
                        type: array
                      featurizer:
                        description: The date time imputer.
                        type: string
                    type: object
                type: object
              profiled:
                description: Profiled is set when we want to create model profile.
                type: boolean
              published:
                description: Published is set when we want to wrap the model in a
                  docker container
                type: boolean
              pushed:
                description: Pushed indicate if the model image should be pushed into
                  the remote docker registry.
                type: boolean
              reported:
                description: Reported is set when a report should be created for this
                  model
                type: boolean
              studyName:
                description: StudyName ref reference the study for this model. IF
                  empty, the model is stand alone
                maxLength: 63
                minLength: 1
                type: string
              task:
                description: Task is the machine learning task (regression, classification).
                  The task is generated from the study task This is a required field.
                  Required
                enum:
                - binary-classification
                - multi-classification
                - multi-label-classification
                - text-classification
                - regression
                - forecasting
                - clustering
                - recommendation
                - outlier-detection
                - novelty-detection
                - topic-modeling
                - auto
                - unknown
                type: string
              tested:
                description: Tested indicate if this model should be testedActual.
                  Default is false. The study controller will set this to true if
                  a model is the best model
                type: boolean
              training:
                description: TrainingSpec is the desired training settings
                properties:
                  checkpointInterval:
                    description: Add snapshot interval for long training time in minutes.
                      This is used to checkpoint training model.
                    format: int32
                    minimum: 0
                    type: integer
                  cv:
                    description: 'If true, this is a cross validation using folds.
                      If False, use the validation set. Default: True'
                    type: boolean
                  cvtype:
                    description: The  type of cross validation. if we have a validation
                      set, we do not do cv.
                    type: string
                  earlyStop:
                    description: 'Early stopping, stop the training after X models
                      with no improvement. Default: False'
                    type: boolean
                  evalMetrics:
                    description: Evaluation metrics are the scores
                    items:
                      description: Metric is the machine learning metric used to evaluate
                        a model performance.
                      enum:
                      - accuracy
                      - average-precision
                      - balanced-accuracy
                      - brier-score-loss
                      - f1
                      - f1-macro
                      - f1-micro
                      - f1-weighted
                      - log-loss
                      - precision
                      - precision-micro
                      - precision-macro
                      - precision-weighted
                      - recall
                      - recall-macro
                      - recall-micro
                      - recall-weighted
                      - auc
                      - fp
                      - fn
                      - tn
                      - tp
                      - explained_variance
                      - mae
                      - mse
                      - msle
                      - median-absolute-error
                      - r2
                      - adjusted-mutual-info-score
                      - adjusted-rand-score
                      - completeness-score
                      - fowlkes-mallows-score
                      - homogeneity-score
                      - mutual-info-score
                      - normalized-mutual-info-score
                      - v-measure-score
                      - rmse
                      - unknown
                      - mape
                      - smape
                      - mdape
                      type: string
                    type: array
                  folds:
                    description: The number of folds during cross validation.
                    format: int32
                    type: integer
                  forecast:
                    description: 'Define the forecast period for time series studies.
                      This is only used in time series models. Default: Empty'
                    properties:
                      backtest:
                        description: The backtest specification, the system supports
                          back testing with expanding windows.
                        properties:
                          initial:
                            description: The initial number of data points, default
                              to 80% of rows.
                            format: int32
                            type: integer
                          windows:
                            description: The number of backtesting windows. Default
                              to 3. can be from 1 to 5.
                            format: int32
                            type: integer
                        type: object
                      confidenceIntervals:
                        description: The confidence levels for the forecast, each
                          level must be between 1-100.
                        format: int32
                        type: integer
                      countryForHoliday:
                        description: Set an holiday schedule for a country.
                        enum:
                        - US
                        - BR
                        - ID
                        - IN
                        - MY
                        - VN
                        - TH
                        - PH
                        - PK
                        - BD
                        - EG
                        - CN
                        - RU
                        - KR
                        - BY
                        - AE
                        - none
                        type: string
                      datetimeFormat:
                        description: The format of the datetime column. Used default
                        type: string
                      dimensionValues:
                        description: a set of value for each partition key
                        items:
                          description: Partition key values are used for the partition
                          properties:
                            key:
                              description: Key is the partition key
                              type: string
                            value:
                              description: Value if the partition value
                              type: string
                          type: object
                        type: array
                      dimensions:
                        description: The list of the dimension columns. If non, the
                          system will treat the whole time series as one.
                        items:
                          type: string
                        type: array
                      forecast:
                        description: Specify if we should generate a forecast using
                          the model If true, the system will perform a forecast and
                          update the forecast connection. Default it true
                        type: boolean
                      forecastConnectionName:
                        description: The name of the connection for a database the
                          result of the forecast If null, the system will insert the
                          forecast in the database.
                        type: string
                      freqSpec:
                        description: Required, the freq of the time series (daily,weekly)
                        properties:
                          interval:
                            description: Default to 1. optional
                            format: int32
                            type: integer
                          unit:
                            description: required
                            enum:
                            - second
                            - minute
                            - hour
                            - day
                            - week
                            - month
                            - quarter
                            - year
                            type: string
                        type: object
                      horizon:
                        description: number of data points to predict in the future.
                          Required.
                        format: int32
                        type: integer
                      repressors:
                        description: List of other columns to take into consideration
                          Default None
                        items:
                          type: string
                        type: array
                      targetColumn:
                        description: The name of the column holding the value. By
                          default this is the target column from the dataset.
                        type: string
                      timeColumn:
                        description: The name of the time column Required.
                        type: string
                    type: object
                  priority:
                    description: 'Priority specify the priority of the model in the
                      training queue. The priority is defined in the model study.
                      Default: 5'
                    format: int32
                    maximum: 10
                    minimum: 1
                    type: integer
                  retry:
                    description: Retry attempt is the number of training retry for
                      this model, Default is 5
                    format: int32
                    type: integer
                  seed:
                    description: 'Used as the random state for an estimator if needed
                      (for example, in RandomForestClassifier) Default: 42'
                    type: number
                  sh:
                    description: Successive halving represent the configuration for
                      the model training, when running the SuccessiveHalvingSpec model
                      search algorithm The metrics are evaluated using the final model,
                      both on the training set and on the test set
                    properties:
                      bracket:
                        description: The bracket of the model when running multi fidelity
                          search.
                        format: int32
                        maximum: 128
                        minimum: 0
                        type: integer
                      budget:
                        description: The budget is used in multi fidelity search.
                          This is the percent of fidelity of the model. The meaning
                          is based on the model configuration. For example fidelity
                          is number of epochs in sgd alg Or can be percent of the
                          data in non sgd algorithm.
                        format: int32
                        maximum: 128
                        minimum: 0
                        type: integer
                      confID:
                        description: The configuration id allocated to this model
                          in rung 0
                        format: int32
                        type: integer
                      modality:
                        description: The modality type. The default modality is based
                          on the type of models For deep models - we use epocs. For
                          classical models - we use data
                        enum:
                        - data
                        - epochs
                        type: string
                      rung:
                        description: The rung of the model
                        format: int32
                        maximum: 128
                        minimum: 0
                        type: integer
                    type: object
                type: object
              versionName:
                description: ServingSiteName is a reference to the product version
                  of this model. This value is based on the study product version
                maxLength: 63
                minLength: 1
                type: string
            type: object
          status:
            description: ModelStatus defines the observed state of the Model
            properties:
              best:
                description: True if this is the best model
                type: boolean
              completionTime:
                description: Represent the time that the model is marked as ready
                format: date-time
                type: string
              conditions:
                items:
                  description: DeploymentCondition describes the state of a deployment
                    at a certain point.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, AutoScaler.
                      type: string
                    type:
                      description: Type of account condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              cost:
                description: The cost of the model
                type: number
              cvScore:
                description: Cross validation score using on the training set.
                type: number
              forecastUri:
                description: The forecast URI
                type: string
              imageName:
                description: The image name of the model
                type: string
              labelsEncoderUri:
                description: The URI of the label encoder binary file, if there is
                  one.
                type: string
              logsUri:
                description: The URI of the log file
                type: string
              manifestUri:
                description: The URI of the manifest in the product bucket.
                type: string
              misclassUri:
                description: A reference to the mis-classification file which were
                  produce during processing
                type: string
              phase:
                description: The phase of the model
                type: string
              profileUri:
                description: ProfileUri is a reference to the visualization uri which
                  were produce during processing
                type: string
              reportName:
                description: Reference to the model report
                type: string
              startTime:
                description: Represents time when the model is first handled by the
                  model controller
                format: date-time
                type: string
              testCompletionTime:
                description: Represents time when the model ended testing be set in
                  happens-before order across separate operations. It is represented
                  in RFC3339 form and is in UTC.
                format: date-time
                type: string
              testResult:
                description: results of training the model (pipeline) on the full
                  training set, and test it on the test set
                items:
                  description: Represent a value for a specific metric
                  properties:
                    metric:
                      description: The metric type name (e.g. F1 / Accuracy)
                      enum:
                      - accuracy
                      - average-precision
                      - balanced-accuracy
                      - brier-score-loss
                      - f1
                      - f1-macro
                      - f1-micro
                      - f1-weighted
                      - log-loss
                      - precision
                      - precision-micro
                      - precision-macro
                      - precision-weighted
                      - recall
                      - recall-macro
                      - recall-micro
                      - recall-weighted
                      - auc
                      - fp
                      - fn
                      - tn
                      - tp
                      - explained_variance
                      - mae
                      - mse
                      - msle
                      - median-absolute-error
                      - r2
                      - adjusted-mutual-info-score
                      - adjusted-rand-score
                      - completeness-score
                      - fowlkes-mallows-score
                      - homogeneity-score
                      - mutual-info-score
                      - normalized-mutual-info-score
                      - v-measure-score
                      - rmse
                      - unknown
                      - mape
                      - smape
                      - mdape
                      type: string
                    value:
                      description: The value for this model
                      type: number
                  type: object
                type: array
              testScore:
                description: TrainingSpec on the full training set, Evaluating on
                  the test set
                type: number
              testStartTime:
                description: Represents time when the model started test on a trainer
                  It is not guaranteed to be set in happens-before order across separate
                  operations. It is represented in RFC3339 form and is in UTC.
                format: date-time
                type: string
              trainCompletionTime:
                description: Represents time when the model ended training
                format: date-time
                type: string
              trainResult:
                description: results of training the model (pipeline) on the full
                  training set, and test it on the training set
                items:
                  description: Represent a value for a specific metric
                  properties:
                    metric:
                      description: The metric type name (e.g. F1 / Accuracy)
                      enum:
                      - accuracy
                      - average-precision
                      - balanced-accuracy
                      - brier-score-loss
                      - f1
                      - f1-macro
                      - f1-micro
                      - f1-weighted
                      - log-loss
                      - precision
                      - precision-micro
                      - precision-macro
                      - precision-weighted
                      - recall
                      - recall-macro
                      - recall-micro
                      - recall-weighted
                      - auc
                      - fp
                      - fn
                      - tn
                      - tp
                      - explained_variance
                      - mae
                      - mse
                      - msle
                      - median-absolute-error
                      - r2
                      - adjusted-mutual-info-score
                      - adjusted-rand-score
                      - completeness-score
                      - fowlkes-mallows-score
                      - homogeneity-score
                      - mutual-info-score
                      - normalized-mutual-info-score
                      - v-measure-score
                      - rmse
                      - unknown
                      - mape
                      - smape
                      - mdape
                      type: string
                    value:
                      description: The value for this model
                      type: number
                  type: object
                type: array
              trainScore:
                description: TrainingSpec on the full training set, Evaluating on
                  the training set
                type: number
              trainStartTime:
                description: Represents time when the model started training.
                format: date-time
                type: string
              weightsUri:
                description: The URI of the model binary file.
                type: string
            required:
            - imageName
            - misclassUri
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
