
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: modelasystems.infra.modela.ai
spec:
  group: infra.modela.ai
  names:
    categories:
    - infra
    - modela
    kind: ModelaSystem
    listKind: ModelaSystemList
    plural: modelasystems
    singular: modelasystem
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ModelaSystemSpec defines the desired state of ModelaSystem
            properties:
              active:
                description: Set the release to active
                type: boolean
              dataImage:
                description: The current software version of modela
                type: string
              images:
                description: DataImage is the container image used to data tasks
                properties:
                  apiGatewayImage:
                    type: string
                  apiproxyImage:
                    description: TrainerImage is the container image used for control
                    type: string
                  batchPredictorImage:
                    description: BatchPredictorImage is used when running batch prediction.
                    type: string
                  cloudproxyImage:
                    description: TrainerImage is the container image used for control
                    type: string
                  controlPlaneImage:
                    description: ControlPlaneImage is the image of the main control
                      plane. the control plane contain all the modela operators
                    type: string
                  databaseProxyImage:
                    description: Database proxy image is a service the translate from
                      kubernetes objects to data base tables.
                    type: string
                  datadockImage:
                    description: Data dock image contain the data ingest service
                    type: string
                  dataplaneImage:
                    description: TrainerImage is the container image used for control
                    type: string
                  frontendImage:
                    description: Frontend image is the image of the frontend web user
                      interface
                    type: string
                  modelaDashboardServerImage:
                    description: The image for the data app dashboard.
                    type: string
                  modelaPredictionServerImage:
                    description: The image used for the prediction server.
                    type: string
                  modelaSystemImage:
                    description: The image for the modela system controller.
                    type: string
                  predictionRouterImage:
                    description: Prediction router image.
                    type: string
                  publisherImage:
                    description: Publisher image defines the image of the publisher.
                      The publisher service is used the package models to tar file
                      or publish them as docker containers.
                    type: string
                  trainerImage:
                    description: TrainerImage is the container image of the trainer
                      service. The trainer is used to train and test models
                    type: string
                required:
                - apiGatewayImage
                - apiproxyImage
                - batchPredictorImage
                - cloudproxyImage
                - controlPlaneImage
                - databaseProxyImage
                - datadockImage
                - dataplaneImage
                - frontendImage
                - modelaDashboardServerImage
                - modelaPredictionServerImage
                - modelaSystemImage
                - predictionRouterImage
                - publisherImage
                - trainerImage
                type: object
            required:
            - dataImage
            - images
            type: object
          status:
            description: ModelaSystemStatus is the observed state of a ModelaSystem
            properties:
              conditions:
                items:
                  description: ModelaSystemCondition describes the state of the license
                    at a certain point.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, AutoScaler.
                      type: string
                    type:
                      description: Type of account condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the Last generation that was acted
                  on
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
