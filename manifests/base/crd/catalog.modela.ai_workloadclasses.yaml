
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: workloadclasses.catalog.modela.ai
spec:
  group: catalog.modela.ai
  names:
    categories:
    - catalog
    - modela
    - all
    kind: WorkloadClass
    listKind: WorkloadClassList
    plural: workloadclasses
    singular: workloadclass
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.vcpu
      name: vCpu
      type: string
    - jsonPath: .spec.mem
      name: Memory
      type: string
    - jsonPath: .spec.gpu
      name: Gpu
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkloadClass define a template for a job.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WorkloadClassSpec defines the specification of  a workload
              class.
            properties:
              frameworks:
                description: List of ml frameworks supported by the data container
                items:
                  type: string
                type: array
              libs:
                description: Libs is the list of python library supported by the data
                  container
                items:
                  properties:
                    name:
                      type: string
                    version:
                      type: string
                  type: object
                type: array
              os:
                description: OS is the name of the os
                type: string
              osVersion:
                description: OSVersion is the version of the os
                type: string
              resourcesTemplate:
                description: Specification for resource
                properties:
                  cpu:
                    default: 1
                    description: Cpu requirment in terms of cpu units
                    type: number
                  gpu:
                    default: 0
                    description: Gpu requirment
                    type: number
                  memory:
                    default: 1000
                    description: Memory requirment in terms of megabytes
                    type: number
                  storage:
                    default: 3600
                    description: Storage requirment in megabytes
                    type: number
                  workloadClassName:
                    description: Workload class name
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
