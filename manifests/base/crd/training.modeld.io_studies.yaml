
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.5.0
  creationTimestamp: null
  name: studies.training.modeld.io
spec:
  group: training.modeld.io
  names:
    categories:
    - training
    - modeld
    kind: Study
    listKind: StudyList
    plural: studies
    shortNames:
    - sd
    singular: study
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .spec.versionName
      name: Version
      type: string
    - jsonPath: .spec.datasetName
      name: Dataset
      type: string
    - jsonPath: .spec.task
      name: Task
      type: string
    - jsonPath: .spec.objective
      name: Objective
      type: string
    - jsonPath: .status.bestModelScore
      name: Score
      type: number
    - jsonPath: .status.bestModel
      name: Best model
      type: string
    - jsonPath: .status.totalTrainers
      name: TotalTrainers
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Study represent a search for the best machine learning model
          using automl.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: StudySpec defines the desired state of the study
            properties:
              aborted:
                default: false
                description: Aborted is set when we want to abort the training
                type: boolean
              datasetName:
                description: DatasetName refer to the dataset object for which the
                  study is for. kubebuilder:default:="" Required.
                type: string
              description:
                description: Description is user provided description kubebuilder:default:=""
                maxLength: 512
                type: string
              hierarchy:
                description: The group hierarchy
                properties:
                  columns:
                    description: The columns in the data frame that belongs to the
                      hierarchy. By default, all the item level column is the last
                      one. The default aggregate function is sum.
                    items:
                      type: string
                    type: array
                  groupLevels:
                    description: The group level columns. For each group level column
                      specify the horizon and the aggregate function
                    items:
                      properties:
                        aggregate:
                          description: The aggregate function used to roll up the
                            lower level
                          enum:
                          - min
                          - max
                          - avg
                          - median
                          type: string
                        distinc:
                          description: The distinct values of the column for this
                            level
                          items:
                            type: string
                          type: array
                        freq:
                          description: The freq of the level
                          enum:
                          - second
                          - minute
                          - hour
                          - day
                          - week
                          - month
                          - quarter
                          - year
                          type: string
                        horizon:
                          description: The number of time periods to
                          format: int32
                          type: integer
                        string:
                          description: The name of the level - the column name
                          type: string
                      type: object
                    type: array
                  itemLevel:
                    description: Item level column.
                    properties:
                      aggregate:
                        description: The aggregate function used to roll up the lower
                          level
                        enum:
                        - min
                        - max
                        - avg
                        - median
                        type: string
                      distinc:
                        description: The distinct values of the column for this level
                        items:
                          type: string
                        type: array
                      freq:
                        description: The freq of the level
                        enum:
                        - second
                        - minute
                        - hour
                        - day
                        - week
                        - month
                        - quarter
                        - year
                        type: string
                      horizon:
                        description: The number of time periods to
                        format: int32
                        type: integer
                      string:
                        description: The name of the level - the column name
                        type: string
                    type: object
                  notifierName:
                    description: Optional notifier to use when the forecast is complete.
                    type: string
                type: object
              labRef:
                description: LabRef is a reference to the lab where the trainers for
                  this study run. If no value is provided, the lab is taken from the
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: 'If referring to a piece of an object instead of
                      an entire object, this string should contain a valid JSON/Go
                      field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within
                      a pod, this would take on a value like: "spec.containers{name}"
                      (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]"
                      (container with index 2 in this pod). This syntax is chosen
                      only to have some well-defined way of referencing a part of
                      an object. TODO: this design is not final and this field is
                      subject to change in the future.'
                    type: string
                  kind:
                    description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                  namespace:
                    description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                    type: string
                  resourceVersion:
                    description: 'Specific resourceVersion to which this reference
                      is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency'
                    type: string
                  uid:
                    description: 'UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids'
                    type: string
                type: object
              location:
                description: The location of the study artifacts By default the bucket
                  is the data product bucket.
                properties:
                  bucketName:
                    type: string
                  path:
                    description: Path to the full data file (e.g. csv file).
                    type: string
                required:
                - bucketName
                - path
                type: object
              modelImagePushed:
                description: Set to true if you want the system to push model image
                  to remote docker registry kubebuilder:default:=false
                type: boolean
              modelPublished:
                default: false
                description: Set to true if you want the system to create a docker
                  model image, at the end of training.
                type: boolean
              objective:
                description: Objective is the objective defined how the study controller
                  will compare model performance.
                enum:
                - accuracy
                - average-precision
                - balanced-accuracy
                - brier-score-loss
                - f1
                - f1-macro
                - f1-micro
                - f1-weighted
                - log-loss
                - precision
                - precision-micro
                - precision-macro
                - precision-weighted
                - recall
                - recall-macro
                - recall-micro
                - recall-weighted
                - auc
                - fp
                - fn
                - tn
                - tp
                - explained_variance
                - mae
                - mse
                - msle
                - median-absolute-error
                - r2
                - adjusted-mutual-info-score
                - adjusted-rand-score
                - completeness-score
                - fowlkes-mallows-score
                - homogeneity-score
                - mutual-info-score
                - normalized-mutual-info-score
                - v-measure-score
                - rmse
                - unknown
                - mape
                - smape
                - mdape
                type: string
              owner:
                description: The owner account name kubebuilder:default:=""
                type: string
              paused:
                default: false
                description: Paused is set when we want to pause the training
                type: boolean
              preprocessing:
                description: 'Preprocessing is template for preprocessors for this
                  study Default: all preprocessing is set to auto.'
                properties:
                  audio:
                    description: Audio specify the pipeline for audio. Not supported
                      in this release
                    properties:
                      columns:
                        description: Name of the datetime columns
                        items:
                          type: string
                        type: array
                      featurizer:
                        description: The date time imputer. kubebuilder:default:="auto"
                        type: string
                    type: object
                  categorical:
                    description: One or more categorical pipelines.
                    properties:
                      columns:
                        description: The columns for this pipeline. Use * to denotes
                          all the columns. Must have at least on value.
                        items:
                          type: string
                        type: array
                      encoder:
                        description: CatEncoder kubebuilder:default:="auto"
                        enum:
                        - one-hot
                        - count-freq
                        - ordinal-encoder
                        - mean
                        - target
                        - woe
                        - rare-label
                        - binary
                        - label
                        - none
                        - auto
                        type: string
                      imputer:
                        description: Categorical varaible imputer kubebuilder:default:="auto"
                        enum:
                        - remove-rows-with-missing-values
                        - mean
                        - median
                        - mode
                        - arbitrary-number
                        - end-tail
                        - random-sample
                        - add-missing-indication
                        - iterative
                        - none
                        - auto
                        - constant
                        type: string
                    type: object
                  datatime:
                    description: DataTime specify the column transformation for datetime
                      columns
                    properties:
                      columns:
                        description: Name of the datetime columns
                        items:
                          type: string
                        type: array
                      expand:
                        description: kubebuilder:default:=false
                        type: boolean
                      imputer:
                        description: The date time imputer. kubebuilder:default:="auto"
                        enum:
                        - remove-rows-with-missing-values
                        - mean
                        - median
                        - mode
                        - arbitrary-number
                        - end-tail
                        - random-sample
                        - add-missing-indication
                        - iterative
                        - none
                        - auto
                        - constant
                        type: string
                    type: object
                  featureSelector:
                    description: FeaturePipeline selector
                    enum:
                    - select-percentile
                    - select-kbest
                    - select-fpr
                    - select-fdr
                    - variance-treshold
                    - none
                    - auto
                    type: string
                  image:
                    description: Image specify the pipeline for images. Not supported
                      in this release
                    properties:
                      columns:
                        description: Name of the datetime columns
                        items:
                          type: string
                        type: array
                      featurizer:
                        description: The date time imputer. kubebuilder:default:="auto"
                        type: string
                    type: object
                  imbalanced:
                    description: Imbalance flag instruct the designer to create an
                      imbalanced pipeline. Default is false
                    type: boolean
                  numeric:
                    description: Numeric specify the column transformation for numeric
                      columns
                    properties:
                      columns:
                        items:
                          type: string
                        type: array
                      imputer:
                        description: Numerical var imputer kubebuilder:default:="auto"
                        enum:
                        - remove-rows-with-missing-values
                        - mean
                        - median
                        - mode
                        - arbitrary-number
                        - end-tail
                        - random-sample
                        - add-missing-indication
                        - iterative
                        - none
                        - auto
                        - constant
                        type: string
                      scaler:
                        description: Numeric var scaler kubebuilder:default:="auto"
                        enum:
                        - standard
                        - max-abs
                        - min-max
                        - normalizer
                        - robust
                        - none
                        - auto
                        type: string
                    type: object
                  reducer:
                    description: Dim reducer
                    enum:
                    - pca
                    - kernel-pca
                    - fast-ica
                    - none
                    - auto
                    type: string
                  text:
                    description: Text specify the column transformation for text columns
                    properties:
                      columns:
                        description: The list of test columns
                        items:
                          type: string
                        type: array
                      embedding:
                        description: If true, the controller will Add word embedding
                          handling to the text pipeline.
                        type: string
                      encoder:
                        description: The text encoder (e.g. tfidf)
                        type: string
                      lemma:
                        description: If true, the controller will Add lemma handling
                          to the text pipeline. kubebuilder:default:=true
                        type: boolean
                      pos:
                        description: kubebuilder:default:=true If true, the controller
                          will Add part of speech handling to the text pipeline.
                        type: boolean
                      stem:
                        description: kubebuilder:default:=true If true, the controller
                          will Add stemmer handling to the text pipeline.
                        type: boolean
                      stopwords:
                        description: If true, the controller will Add stop word handling
                          to the text pipeline. kubebuilder:default:=true
                        type: boolean
                      tokenizer:
                        description: Text tokenizer
                        type: string
                    type: object
                  video:
                    description: Video specify the pipeline for video. Not supported
                      in this release
                    properties:
                      columns:
                        description: Name of the datetime columns
                        items:
                          type: string
                        type: array
                      featurizer:
                        description: The date time imputer. kubebuilder:default:="auto"
                        type: string
                    type: object
                type: object
              profiled:
                default: false
                description: Profiled is set when we want to create model profile
                  and study profile.
                type: boolean
              reported:
                default: false
                description: Reported is set when we want to create model report
                type: boolean
              search:
                description: Search defines the model search
                properties:
                  allowlist:
                    description: AllowList contain the list of algorithms that should
                      be tested as part of the search.
                    items:
                      enum:
                      - knn-regressor
                      - ridge-regressor
                      - lasso-regressor
                      - ada-boost-regressor
                      - decision-tree-regressor
                      - extra-tree-regressor
                      - linear-svr
                      - svr
                      - passive-agressive-regressor
                      - sgd-regressor
                      - gradient-boosting-regressor
                      - random-forest-regressor
                      - xgb-regressor
                      - catboost-regressor
                      - sgd-regressor
                      - gradient-boosting-regressor
                      - random-forest-regressor
                      - xgb-regressor
                      - catboost-regressor
                      - lightgbm-regressor
                      - elasticnet-regressor
                      - dnn-regressor
                      - elliptic-envelope
                      - one-class-svm
                      - isolation-forest
                      - local-outlier-factor
                      - prophet
                      - knn-classifier
                      - ada-boost-classifier
                      - bernoulli-nb
                      - decision-tree-classifier
                      - extra-tree-classifier
                      - linear-svc
                      - svc
                      - passive-aggressive-classifier
                      - sgd-classifier
                      - logistic-regression
                      - gradient-boosting-classifier
                      - random-forest-classifier
                      - xgboost-classifier
                      - ridge-classifier
                      - quadratic-discriminant
                      - linear-discriminant
                      - light-gbm
                      - catboost-classifier
                      - stacking-ensemble
                      type: string
                    type: array
                  maxCost:
                    description: MaxCost specify what is the maximum cost incurred
                      before stopping model creations kubebuilder:default:=100
                    format: int32
                    maximum: 1000
                    minimum: 1
                    type: integer
                  maxModels:
                    description: Used for random search, the max models sampled. kubebuilder:default:=10
                    format: int32
                    maximum: 1000
                    minimum: 1
                    type: integer
                  maxTime:
                    description: MaxTime specify what is the maximum time allocated
                      to a study (in minutes). the cross validation stage. kubebuilder:default:=30
                    format: int32
                    maximum: 10000
                    minimum: 1
                    type: integer
                  minScore:
                    description: The minimum score by which the search would stop
                      kubebuilder:default:=0
                    type: number
                  resources:
                    description: Define the general size of the resources needed for
                      each trainer.
                    properties:
                      cpu:
                        description: Cpu specify the cpu requirements will be compared
                          kubebuilder:default:="M"
                        enum:
                        - XS
                        - S
                        - M
                        - L
                        - XL
                        - XXL
                        - XXXL
                        - AutoScaler
                        type: string
                      gpu:
                        description: Gpu specify the desired gpu requirements.  will
                          be compared kubebuilder:default:="M"
                        enum:
                        - XS
                        - S
                        - M
                        - L
                        - XL
                        - XXL
                        - XXXL
                        - AutoScaler
                        type: string
                      mem:
                        description: Mem define the memory resource requirements will
                          be compared kubebuilder:default:="M"
                        enum:
                        - XS
                        - S
                        - M
                        - L
                        - XL
                        - XXL
                        - XXXL
                        - AutoScaler
                        type: string
                    type: object
                  retainTop:
                    description: Indicate the total number of full models that would
                      be retain in etcd. All other models are garbage collected (archived).
                      models are sorted by thier objective score. kubebuilder:default:=10
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  retainedFor:
                    description: RetainFor measure the time in minutes for modeld
                      trained. Default is 60 min (1 H). kubebuilder:default:=60
                    format: int32
                    maximum: 2400
                    minimum: 0
                    type: integer
                  shOptions:
                    description: SHOptions is the desired options for successive halving
                      search. All other models are saved into an archive.
                    properties:
                      eliminationRate:
                        description: The rate of elimination during SH search, such
                          that only 1/rate of models are promoted to the next half
                          kubebuilder:default:=3
                        format: int32
                        type: integer
                      maxBudget:
                        description: The maximum budget allocated to each model during
                          SH search. The default max budget is 81 kubebuilder:default:=81
                        format: int32
                        type: integer
                      modality:
                        description: The modality type. The default modality is based
                          on the type of models For deep models - we use epocs. For
                          classical models - we use data kubebuilder:default:="epocs"
                        enum:
                        - data
                        - epochs
                        type: string
                    type: object
                  stackingEnsemble:
                    description: StackingEnsemble If true, create a stacking ensemble
                      of the top 3 models. kubebuilder:default:=true
                    type: boolean
                  test:
                    description: Test indicate the desired number of models that should
                      be passed to the testing phase. kubebuilder:default:=80
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  trainers:
                    description: The desired number of trainers running during search.
                      kubebuilder:default:=1
                    format: int32
                    maximum: 50
                    type: integer
                  type:
                    description: Type specify the hyper parameter optimization search
                      method. The only supported value is random kubebuilder:default:='random'
                    enum:
                    - random
                    - grid
                    - bayesian
                    - sh
                    - manual
                    - auto
                    type: string
                  votingEnsemble:
                    description: VotingEnsample - If true, create a voting ensemble
                      of the top 3 models. kubebuilder:default:=false
                    type: boolean
                type: object
              split:
                description: Split is reference to the split specification
                properties:
                  auto:
                    description: Indicate that this is an automatic split. kubebuilder:default:=true
                    type: boolean
                  seed:
                    description: 'The seed to use for the estimator Default: 42 kubebuilder:default:=42'
                    type: number
                  splitColumn:
                    description: The name of the column used to split
                    type: string
                  splitPolicy:
                    description: Indicate if the dataset split should be stratified.
                      kubebuilder:default:="stratified"
                    type: string
                  test:
                    description: Test is percent of dataset rows which would be used
                      to compute the objective during kubebuilder:default:=20
                    format: int32
                    maximum: 50
                    minimum: 0
                    type: integer
                  train:
                    description: Training is a percent number (0-100) which specify
                      how much of the data will be used for training kubebuilder:default:=80
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  validation:
                    description: Validation is percent of dataset rows which would
                      be used to compute the objective during hyper parameter search
                      phase. Only used if we do not do cross validation. default is
                      10% of the data, if we do not have cross validation. default
                      is 0% of the data, if we do cross validation. kubebuilder:default:=0
                    format: int32
                    maximum: 50
                    minimum: 0
                    type: integer
                type: object
              task:
                description: Task specify the machine learning task (e.g classification).
                  This must match the task of the data product. Required.
                enum:
                - binary-classification
                - multi-classification
                - multi-label-classification
                - text-classification
                - regression
                - forecasting
                - clustering
                - recommendation
                - outlier-detection
                - novelty-detection
                - topic-modeling
                - auto
                - unknown
                type: string
              trainerClassRef:
                description: 'WorkloadClassRef is a reference to the workload class
                  object from the catalog. Default : scikit learn trainer.'
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: 'If referring to a piece of an object instead of
                      an entire object, this string should contain a valid JSON/Go
                      field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within
                      a pod, this would take on a value like: "spec.containers{name}"
                      (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]"
                      (container with index 2 in this pod). This syntax is chosen
                      only to have some well-defined way of referencing a part of
                      an object. TODO: this design is not final and this field is
                      subject to change in the future.'
                    type: string
                  kind:
                    description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                  namespace:
                    description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                    type: string
                  resourceVersion:
                    description: 'Specific resourceVersion to which this reference
                      is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency'
                    type: string
                  uid:
                    description: 'UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids'
                    type: string
                type: object
              training:
                description: Training template contain the desired training parameter
                  for the models.
                properties:
                  checkpointInterval:
                    description: Add snapshot interval for long training time in minutes.
                      This is used to checkpoint training model.
                    format: int32
                    minimum: 0
                    type: integer
                  cv:
                    description: 'If true, this is a cross validation using folds.
                      If False, use the validation set. Default: True'
                    type: boolean
                  cvtype:
                    description: The  type of cross validation. if we have a validation
                      set, we do not do cv.
                    type: string
                  earlyStop:
                    description: 'Early stopping, stop the training after X models
                      with no improvement. Default: False'
                    type: boolean
                  evalMetrics:
                    description: Evaluation metrics are the scores
                    items:
                      description: Metric is the machine learning metric used to evaluate
                        a model performance.
                      enum:
                      - accuracy
                      - average-precision
                      - balanced-accuracy
                      - brier-score-loss
                      - f1
                      - f1-macro
                      - f1-micro
                      - f1-weighted
                      - log-loss
                      - precision
                      - precision-micro
                      - precision-macro
                      - precision-weighted
                      - recall
                      - recall-macro
                      - recall-micro
                      - recall-weighted
                      - auc
                      - fp
                      - fn
                      - tn
                      - tp
                      - explained_variance
                      - mae
                      - mse
                      - msle
                      - median-absolute-error
                      - r2
                      - adjusted-mutual-info-score
                      - adjusted-rand-score
                      - completeness-score
                      - fowlkes-mallows-score
                      - homogeneity-score
                      - mutual-info-score
                      - normalized-mutual-info-score
                      - v-measure-score
                      - rmse
                      - unknown
                      - mape
                      - smape
                      - mdape
                      type: string
                    type: array
                  folds:
                    description: The number of folds during cross validation.
                    format: int32
                    type: integer
                  forecast:
                    description: 'Define the forecast period for time series studies.
                      This is only used in time series models. Default: Empty'
                    properties:
                      backtest:
                        description: The backtest specification, the system supports
                          back testing with expanding windows.
                        properties:
                          initial:
                            description: The initial number of data points, default
                              to 80% of rows. kubebuilder:default:=80
                            format: int32
                            type: integer
                          windows:
                            description: The number of backtesting windows. Default
                              to 3. can be from 1 to 5. kubebuilder:default:=3
                            format: int32
                            type: integer
                        type: object
                      confidenceIntervals:
                        description: The confidence levels for the forecast, each
                          level must be between 1-100.
                        format: int32
                        type: integer
                      countryForHoliday:
                        description: Set an holiday schedule for a country.
                        enum:
                        - US
                        - BR
                        - ID
                        - IN
                        - MY
                        - VN
                        - TH
                        - PH
                        - PK
                        - BD
                        - EG
                        - CN
                        - RU
                        - KR
                        - BY
                        - AE
                        - none
                        type: string
                      datetimeFormat:
                        description: The format of the datetime column. Used default
                        type: string
                      dimensionValues:
                        description: a set of value for each partition key
                        items:
                          description: DimensionValue specify the partition key values
                            are used for the partition
                          properties:
                            key:
                              description: Key is the partition key
                              type: string
                            value:
                              description: Value if the partition value
                              type: string
                          type: object
                        type: array
                      dimensions:
                        description: The list of the dimension columns. If non, the
                          system will treat the whole time series as one.
                        items:
                          type: string
                        type: array
                      forecast:
                        description: Specify if we should generate a forecast using
                          the model If true, the system will perform a forecast and
                          update the forecast connection. Default it true
                        type: boolean
                      forecastConnectionName:
                        description: The name of the connection for a database the
                          result of the forecast If null, the system will insert the
                          forecast in the database.
                        type: string
                      freqSpec:
                        description: Required, the freq of the time series (daily,weekly)
                        properties:
                          interval:
                            description: Default to 1. kubebuilder:default:=1 optional
                            format: int32
                            type: integer
                          unit:
                            description: required
                            enum:
                            - second
                            - minute
                            - hour
                            - day
                            - week
                            - month
                            - quarter
                            - year
                            type: string
                        type: object
                      horizon:
                        description: Horizon is the number of data points to predict
                          in the future. Required.
                        format: int32
                        type: integer
                      repressors:
                        description: List of other columns to take into consideration
                          Default None
                        items:
                          type: string
                        type: array
                      targetColumn:
                        description: The name of the column holding the value. By
                          default this is the target column from the dataset.
                        type: string
                      timeColumn:
                        description: The name of the time column Required.
                        type: string
                    type: object
                  priority:
                    description: 'Priority specify the priority of the model in the
                      training queue. The priority is defined in the model study.
                      Default: 5'
                    format: int32
                    maximum: 10
                    minimum: 1
                    type: integer
                  retry:
                    description: Retry attempt is the number of training retry for
                      this model, Default is 5
                    format: int32
                    type: integer
                  seed:
                    description: 'Used as the random state for an estimator if needed
                      (for example, in RandomForestClassifier) Default: 42'
                    type: number
                  sh:
                    description: Successive halving represent the configuration for
                      the model training, when running the SuccessiveHalvingSpec model
                      search algorithm The metrics are evaluated using the final model,
                      both on the training set and on the test set
                    properties:
                      bracket:
                        description: The bracket of the model when running multi fidelity
                          search.
                        format: int32
                        maximum: 128
                        minimum: 0
                        type: integer
                      budget:
                        description: The budget is used in multi fidelity search.
                          This is the percent of fidelity of the model. The meaning
                          is based on the model configuration. For example fidelity
                          is number of epochs in sgd alg Or can be percent of the
                          data in non sgd algorithm.
                        format: int32
                        maximum: 128
                        minimum: 0
                        type: integer
                      confID:
                        description: The configuration id allocated to this model
                          in rung 0
                        format: int32
                        type: integer
                      modality:
                        description: The modality type. The default modality is based
                          on the type of models For deep models - we use epocs. For
                          classical models - we use data
                        enum:
                        - data
                        - epochs
                        type: string
                      rung:
                        description: The rung of the model
                        format: int32
                        maximum: 128
                        minimum: 0
                        type: integer
                    type: object
                type: object
              versionName:
                description: VersionName is the data product version of the study
                  required
                maxLength: 63
                minLength: 1
                type: string
            required:
            - datasetName
            - task
            - versionName
            type: object
          status:
            description: StudyStatus defines the observed state of the Study
            properties:
              bestModel:
                description: The Best model name.
                type: string
              bestModelScore:
                description: The Best model score, so far.
                type: number
              busyTrainers:
                description: TotalTrainers training
                format: int32
                type: integer
              completionTime:
                description: The study end time.
                format: date-time
                type: string
              conditions:
                description: This is the set of partition levels Represents the latest
                  available observations of a study state.
                items:
                  description: StudyCondition describes the state of a StudyName.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, AutoScaler.
                      type: string
                    type:
                      description: Type of study condition.
                      type: string
                  type: object
                type: array
              failedTestingModels:
                description: models failed testing.
                format: int32
                type: integer
              failedTrainingModels:
                description: models that failed during training.
                format: int32
                type: integer
              freeTrainers:
                description: Free trainers
                format: int32
                type: integer
              models:
                description: Total models created for the study
                format: int32
                type: integer
              phase:
                description: The phase of the study
                type: string
              profileUri:
                description: A reference to the profile uri which were produce during
                  processing
                type: string
              reportName:
                description: The study report name
                type: string
              startTime:
                description: Study start time
                format: date-time
                type: string
              testedModels:
                description: Tested models
                format: int32
                type: integer
              testingCompletionTime:
                description: Represents time when the study reached a successful complete
                  or failure phase. It is not guaranteed to
                format: date-time
                type: string
              testingModels:
                description: models in testing
                format: int32
                type: integer
              testingStarted:
                description: Start of the testing phase
                format: date-time
                type: string
              trainedModels:
                description: models that were successful during training
                format: int32
                type: integer
              trainers:
                description: Total number of trainers in this study
                format: int32
                type: integer
              trainingCompletionTime:
                description: End of the training phase.
                format: date-time
                type: string
              trainingModels:
                description: models which are currently training or testing.
                format: int32
                type: integer
              trainingStartTime:
                description: Start of the training phase
                format: date-time
                type: string
              waitingToTestModels:
                description: models waiting testing
                format: int32
                type: integer
              waitingToTrain:
                description: models waiting for a trainer
                format: int32
                type: integer
            required:
            - phase
            - profileUri
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
